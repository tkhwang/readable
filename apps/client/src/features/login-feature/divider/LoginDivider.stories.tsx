import React from 'react';
import { LoginDivider } from './LoginDivider';

export default {
  component: LoginDivider,
  title: 'login/LoginDivider',
};

const Template = args => <LoginDivider {...args} />;

export const Default = Template.bind({});
